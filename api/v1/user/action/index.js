var root = require('./root');
var getUserAll = require('./getUserAll');
var postUser = require('./postUser');
var putUserId = require('./putUserId');
var getUserId = require('./getUserId');
var deleteUserId = require('./deleteUserId');

module.exports = {
    root,
    getUserAll,
    postUser,
    putUserId,
    getUserId,
    deleteUserId
}